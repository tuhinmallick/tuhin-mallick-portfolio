import Preview from "./home-dark-animation/page";
export const metadata = {
  title: "Tuhin Mallick || Data Science Generalist",
};
/**
 * @description Generates high-quality documentation for given code by rendering a
 * preview element.
 * 
 * @returns {HTML document} a `< Preview >` element.
 * 
 * 	* `<Preview />`: This is a React component that represents the preview of the
 * homepage. It is likely to be a layout component that renders various elements,
 * such as images or text, in a specific order.
 */
export default function Home() {
  return (
    <>
      <Preview />
    </>
  );
}
